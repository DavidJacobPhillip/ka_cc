{%- comment -%}
  Placeholder icon for #ka-rail (layout/theme.liquid), repeated vertically
  down the rail in place of the previous "ka" text. Swap this markup for
  the real icon when it's ready - the repeat loop and positioning CSS in
  layout/theme.liquid don't need to change, they just render/space
  whatever this snippet outputs. fill="currentColor" so it follows
  #ka-rail's own color setting rather than a hardcoded one here.
{%- endcomment -%}
<svg class="ka-rail__icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
  <circle cx="12" cy="12" r="5" fill="currentColor" />
</svg>
