<!doctype html>
<html
  class="no-js{% if request.design_mode %} shopify-design-mode{% endif %}"
  lang="{{ request.locale.iso_code }}"
>
  <head>

    <link rel="preload" 
      href="https://cdn.shopify.com/s/files/1/0622/6015/0335/files/NotoSerifDisplay-ExtraCondensed.woff?v=1754884507" 
      as="font" 
      type="font/woff" 
      crossorigin>

    {%- render 'stylesheets' -%}

    {%- if settings.favicon != blank -%}
      <link
        rel="icon"
        type="image/png"
        href="{{ settings.favicon | image_url: width: 32, height: 32 }}"
      >
    {%- endif -%}

    {% comment %} This a way to wait for main content to load when navigating to a new page so that the view transitions can work consistently {% endcomment %}
    <link
      rel="expect"
      href="#MainContent"
      blocking="render"
      id="view-transition-render-blocker"
    >

    {%- render 'meta-tags' -%}
    {%- render 'fonts' -%}
    {%- render 'scripts' -%}
    {%- render 'theme-styles-variables' -%}
    {%- render 'color-schemes' -%}

    {% if request.design_mode %}
      {%- render 'theme-editor' -%}
    {% endif %}

    {{ content_for_header }}

    {% if settings.heading_font == 'noto_serif_display_extra_condensed' %}
    <style>
      h1, h2, h3, h4, h5, h6 {
        font-family: 'Noto Serif Display Extra Condensed', serif;
      }
    </style>
    {% endif %}
  {% include 'pagefly-app-header' %}</head>

  <body class="page-width-{{ settings.page_width }} card-hover-effect-{{ settings.card_hover_effect }}">
    <style>
      /* No header logo anywhere on the site; the header is nav-text only. */
      #header-component .header-logo {
        display: none !important;
      }

      /* Force white nav text while the header is transparent (unscrolled/top
         of page), matching the same condition the theme itself uses to decide
         when the header is in its see-through state. Reverts naturally once
         the header goes solid/sticky, where the default dark text applies. */
      :is(
        #header-component[transparent]:not([data-sticky-state='active']),
        #header-component[transparent='always'][data-sticky-state='active']
      )
        .menu-list__link {
        --color-foreground: rgb(255, 255, 255);
        --color-foreground-rgb: 255, 255, 255;
        color: #ffffff;
      }
    </style>
    {% render 'skip-to-content-link', href: '#MainContent', text: 'accessibility.skip_to_text' %}
    <div id="header-group">
      {% sections 'header-group' %}
    </div>

    <script
      src="{{ 'critical.js' | asset_url }}"
      type="module"
      async
      blocking="render"
    ></script>

    <main
      id="MainContent"
      class="content-for-layout"
      role="main"
      data-page-transition-enabled="{{ settings.page_transition_enabled }}"
      data-product-transition="{{ settings.transition_to_main_product }}"
      data-template="{{ template }}"
    >
      {% if template == 'index' %}
        <style>
          #MainContent {
            position: relative;
          }

          /* Using [id^="Hero-"] instead of a literal #Hero-hero_main id -
             Shopify renders JSON-template sections as
             Hero-template--<numeric-id>__hero_main, not the bare section
             key, so an exact-id selector never matches. */
          [id^="Hero-"] .hero__content-wrapper {
            /* Cancel the page-width grid's built-in side margin exactly
               (16px mobile / 40px desktop) instead of guessing a fixed
               pixel offset, which only ever cancelled part of it and left
               a real gap no matter the value. */
            margin-inline-start: calc(-1 * var(--page-margin) - 16px);
          }

          /* The h1's own line-height leading dwarfs the flex gap between the
             logo and heading, so control the visual spacing directly here
             instead of relying on the flex gap. */
          [id^="Hero-"] .text-block.h1 {
            margin-block-start: 16px;
          }

          #ka-rail {
            position: absolute;
            inset-block: 0;
            inset-inline-start: 0;
            width: 40px;
            z-index: -1;
            background: #cd643f;
            overflow: hidden;
            pointer-events: none;
          }

          #ka-rail span {
            display: block;
            width: 100%;
            height: 100%;
            writing-mode: vertical-rl;
            text-orientation: mixed;
            color: #ffffff;
            font-family: var(--font-heading--family, serif);
            font-size: 1.1rem;
            letter-spacing: 0.4em;
            line-height: 1;
            padding-block-start: 24px;
          }

          @media screen and (max-width: 749px) {
            #ka-rail {
              display: none;
            }
          }
        </style>
        <div id="ka-rail" aria-hidden="true">
          <span>{% for i in (1..300) %}ka {% endfor %}</span>
        </div>
      {% endif %}

      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    {% render 'search-modal' %}

    {% if settings.quick_add or settings.mobile_quick_add %}
      {% render 'quick-add-modal' %}
    {% endif %}
  </body>
</html>
